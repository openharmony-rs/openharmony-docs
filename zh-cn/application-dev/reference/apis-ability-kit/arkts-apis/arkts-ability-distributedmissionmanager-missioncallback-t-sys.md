# MissionCallback（系统接口）

```TypeScript
export type MissionCallback = _MissionCallback
```

作为可以[registerMissionListener](arkts-ability-distributedmissionmanager-registermissionlistener-f-sys.md#registermissionlistener-1)的入参，表示开始同步后，建立的回调函数。

**起始版本：** 10

**模型约束：** 此接口仅可在Stage模型下使用。

<!--Device-distributedMissionManager-export type MissionCallback = _MissionCallback--><!--Device-distributedMissionManager-export type MissionCallback = _MissionCallback-End-->

**系统能力：** SystemCapability.Ability.AbilityRuntime.Mission

**系统接口：** 此接口为系统接口。

**属性类型：** _MissionCallback

