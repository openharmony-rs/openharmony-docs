# PageNodeInfo（系统接口）

```TypeScript
export type PageNodeInfo = _PageNodeInfo.default
```

自动填充的页面节点信息。

**起始版本：** 11

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Ability.AbilityRuntime.AbilityCore

**系统接口：** 此接口为系统接口。

**属性类型：** _PageNodeInfo.default

