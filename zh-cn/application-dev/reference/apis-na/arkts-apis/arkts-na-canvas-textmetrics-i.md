# TextMetrics

文本的尺寸信息。

**起始版本：** 23

**ArkTS模式：** 仅支持ArkTS-Sta，起始版本为23。

**废弃版本：** -1

<!--Device-unnamed-export declare interface TextMetrics--><!--Device-unnamed-export declare interface TextMetrics-End-->

**系统能力：** SystemCapability.ArkUI.ArkUI.Full

## actualBoundingBoxAscent

```TypeScript
readonly actualBoundingBoxAscent: double
```

只读属性，从CanvasRenderingContext2D.textBaseline属性标明的水平线到渲染文本的矩形边界顶部的距离。

**类型：** double

**起始版本：** 23

**ArkTS模式：** 仅支持ArkTS-Sta，起始版本为23。

**废弃版本：** -1

**模型约束：** 此接口仅可在Stage模型下使用。

<!--Device-TextMetrics-readonly actualBoundingBoxAscent: double--><!--Device-TextMetrics-readonly actualBoundingBoxAscent: double-End-->

**系统能力：** SystemCapability.ArkUI.ArkUI.Full

## actualBoundingBoxDescent

```TypeScript
readonly actualBoundingBoxDescent: double
```

只读属性，从CanvasRenderingContext2D.textBaseline属性标明的水平线到渲染文本的矩形边界底部的距离。

**类型：** double

**起始版本：** 23

**ArkTS模式：** 仅支持ArkTS-Sta，起始版本为23。

**废弃版本：** -1

**模型约束：** 此接口仅可在Stage模型下使用。

<!--Device-TextMetrics-readonly actualBoundingBoxDescent: double--><!--Device-TextMetrics-readonly actualBoundingBoxDescent: double-End-->

**系统能力：** SystemCapability.ArkUI.ArkUI.Full

## actualBoundingBoxLeft

```TypeScript
readonly actualBoundingBoxLeft: double
```

只读属性，平行于基线，从CanvasRenderingContext2D.textAlign属性确定的对齐点到文本矩形边界左侧的距离。

**类型：** double

**起始版本：** 23

**ArkTS模式：** 仅支持ArkTS-Sta，起始版本为23。

**废弃版本：** -1

**模型约束：** 此接口仅可在Stage模型下使用。

<!--Device-TextMetrics-readonly actualBoundingBoxLeft: double--><!--Device-TextMetrics-readonly actualBoundingBoxLeft: double-End-->

**系统能力：** SystemCapability.ArkUI.ArkUI.Full

## actualBoundingBoxRight

```TypeScript
readonly actualBoundingBoxRight: double
```

只读属性，平行于基线，从CanvasRenderingContext2D.textAlign属性确定的对齐点到文本矩形边界右侧的距离。

**类型：** double

**起始版本：** 23

**ArkTS模式：** 仅支持ArkTS-Sta，起始版本为23。

**废弃版本：** -1

**模型约束：** 此接口仅可在Stage模型下使用。

<!--Device-TextMetrics-readonly actualBoundingBoxRight: double--><!--Device-TextMetrics-readonly actualBoundingBoxRight: double-End-->

**系统能力：** SystemCapability.ArkUI.ArkUI.Full

## alphabeticBaseline

```TypeScript
readonly alphabeticBaseline: double
```

只读属性，从CanvasRenderingContext2D.textBaseline属性标明的水平线到线框的alphabetic基线的距离。

**类型：** double

**起始版本：** 23

**ArkTS模式：** 仅支持ArkTS-Sta，起始版本为23。

**废弃版本：** -1

**模型约束：** 此接口仅可在Stage模型下使用。

<!--Device-TextMetrics-readonly alphabeticBaseline: double--><!--Device-TextMetrics-readonly alphabeticBaseline: double-End-->

**系统能力：** SystemCapability.ArkUI.ArkUI.Full

## emHeightAscent

```TypeScript
readonly emHeightAscent: double
```

只读属性，从CanvasRenderingContext2D.textBaseline属性标明的水平线到线框中em方块顶部的距离。

**类型：** double

**起始版本：** 23

**ArkTS模式：** 仅支持ArkTS-Sta，起始版本为23。

**废弃版本：** -1

**模型约束：** 此接口仅可在Stage模型下使用。

<!--Device-TextMetrics-readonly emHeightAscent: double--><!--Device-TextMetrics-readonly emHeightAscent: double-End-->

**系统能力：** SystemCapability.ArkUI.ArkUI.Full

## emHeightDescent

```TypeScript
readonly emHeightDescent: double
```

只读属性，从CanvasRenderingContext2D.textBaseline属性标明的水平线到线框中em方块底部的距离。

**类型：** double

**起始版本：** 23

**ArkTS模式：** 仅支持ArkTS-Sta，起始版本为23。

**废弃版本：** -1

**模型约束：** 此接口仅可在Stage模型下使用。

<!--Device-TextMetrics-readonly emHeightDescent: double--><!--Device-TextMetrics-readonly emHeightDescent: double-End-->

**系统能力：** SystemCapability.ArkUI.ArkUI.Full

## fontBoundingBoxAscent

```TypeScript
readonly fontBoundingBoxAscent: double
```

只读属性，从CanvasRenderingContext2D.textBaseline属性标明的水平线到渲染文本的所有字体的矩形最高边界顶部的距离。

**类型：** double

**起始版本：** 23

**ArkTS模式：** 仅支持ArkTS-Sta，起始版本为23。

**废弃版本：** -1

**模型约束：** 此接口仅可在Stage模型下使用。

<!--Device-TextMetrics-readonly fontBoundingBoxAscent: double--><!--Device-TextMetrics-readonly fontBoundingBoxAscent: double-End-->

**系统能力：** SystemCapability.ArkUI.ArkUI.Full

## fontBoundingBoxDescent

```TypeScript
readonly fontBoundingBoxDescent: double
```

只读属性，从CanvasRenderingContext2D.textBaseline属性标明的水平线到渲染文本的所有字体的矩形边界最底部的距离。

**类型：** double

**起始版本：** 23

**ArkTS模式：** 仅支持ArkTS-Sta，起始版本为23。

**废弃版本：** -1

**模型约束：** 此接口仅可在Stage模型下使用。

<!--Device-TextMetrics-readonly fontBoundingBoxDescent: double--><!--Device-TextMetrics-readonly fontBoundingBoxDescent: double-End-->

**系统能力：** SystemCapability.ArkUI.ArkUI.Full

## hangingBaseline

```TypeScript
readonly hangingBaseline: double
```

只读属性，从CanvasRenderingContext2D.textBaseline属性标明的水平线到线框的hanging基线的距离。

**类型：** double

**起始版本：** 23

**ArkTS模式：** 仅支持ArkTS-Sta，起始版本为23。

**废弃版本：** -1

**模型约束：** 此接口仅可在Stage模型下使用。

<!--Device-TextMetrics-readonly hangingBaseline: double--><!--Device-TextMetrics-readonly hangingBaseline: double-End-->

**系统能力：** SystemCapability.ArkUI.ArkUI.Full

## height

```TypeScript
readonly height: double
```

只读属性，文本方块的高度。

**类型：** double

**起始版本：** 23

**ArkTS模式：** 仅支持ArkTS-Sta，起始版本为23。

**废弃版本：** -1

**模型约束：** 此接口仅可在Stage模型下使用。

<!--Device-TextMetrics-readonly height: double--><!--Device-TextMetrics-readonly height: double-End-->

**系统能力：** SystemCapability.ArkUI.ArkUI.Full

## ideographicBaseline

```TypeScript
readonly ideographicBaseline: double
```

只读属性，从CanvasRenderingContext2D.textBaseline属性标明的水平线到线框的ideographic基线的距离。

**类型：** double

**起始版本：** 23

**ArkTS模式：** 仅支持ArkTS-Sta，起始版本为23。

**废弃版本：** -1

**模型约束：** 此接口仅可在Stage模型下使用。

<!--Device-TextMetrics-readonly ideographicBaseline: double--><!--Device-TextMetrics-readonly ideographicBaseline: double-End-->

**系统能力：** SystemCapability.ArkUI.ArkUI.Full

## width

```TypeScript
readonly width: double
```

只读属性，文本方块的宽度。

**类型：** double

**起始版本：** 23

**ArkTS模式：** 仅支持ArkTS-Sta，起始版本为23。

**废弃版本：** -1

**模型约束：** 此接口仅可在Stage模型下使用。

<!--Device-TextMetrics-readonly width: double--><!--Device-TextMetrics-readonly width: double-End-->

**系统能力：** SystemCapability.ArkUI.ArkUI.Full

