# OnLazyLoadingFunc

```TypeScript
type OnLazyLoadingFunc = (index: int) => void
```

懒加载函数类型。

**起始版本：** 23

**ArkTS模式：** 仅支持ArkTS-Sta，起始版本为23。

**废弃版本：** -1

**模型约束：** 此接口仅可在Stage模型下使用。

<!--Device-unnamed-type OnLazyLoadingFunc = (index: int) => void--><!--Device-unnamed-type OnLazyLoadingFunc = (index: int) => void-End-->

**系统能力：** SystemCapability.ArkUI.ArkUI.Full

**参数：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| index | int | 是 | 当前数据项对应的索引。 |

