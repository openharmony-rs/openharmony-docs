# DoubleLengthDetents

```TypeScript
export type DoubleLengthDetents = [
    (SheetSize | Length),
    SheetSize | Length | undefined
]
```

定义了两个高度的挡位。

**起始版本：** 23

**ArkTS模式：** 仅支持ArkTS-Sta，起始版本为23。

**废弃版本：** -1

**模型约束：** 此接口仅可在Stage模型下使用。

<!--Device-unnamed-export type DoubleLengthDetents = [    (SheetSize | Length),    SheetSize | Length | undefined]--><!--Device-unnamed-export type DoubleLengthDetents = [    (SheetSize | Length),    SheetSize | Length | undefined]-End-->

**系统能力：** SystemCapability.ArkUI.ArkUI.Full

**属性类型：** [
    (SheetSize | Length),
    SheetSize | Length | undefined
]

