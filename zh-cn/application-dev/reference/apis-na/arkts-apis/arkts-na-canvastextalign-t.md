# CanvasTextAlign

```TypeScript
export type CanvasTextAlign = 'center' | 'end' | 'left' | 'right' | 'start'
```

定义文本对齐方式的类型。ltr布局模式下'start'和'left'一致，rtl布局模式下'start'和'right'一致。 'center': 文本居中对齐。 'start': 文本对齐界线开始的地方。 'end': 文本对齐界线结束的地方。 'left': 文本左对齐。 'right': 文本右对齐。

**起始版本：** 23

**ArkTS模式：** 同时支持ArkTS-Dyn、ArkTS-Sta，起始版本为23。

**模型约束：** 此接口仅可在Stage模型下使用。

<!--Device-unnamed-export type CanvasTextAlign = 'center' | 'end' | 'left' | 'right' | 'start'--><!--Device-unnamed-export type CanvasTextAlign = 'center' | 'end' | 'left' | 'right' | 'start'-End-->

**系统能力：** SystemCapability.ArkUI.ArkUI.Full

| 类型 | 说明 |
| --- | --- |
| 'center' |  |
| 'end' |  |
| 'left' |  |
| 'right' |  |
| 'start' |  |

