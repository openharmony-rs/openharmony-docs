# UserAgentFormFactor

用户设备形态。

**起始版本：** 24

**系统能力：** SystemCapability.Web.Webview.Core

## AUTOMOTIVE

```TypeScript
AUTOMOTIVE = 'Automotive'
```

车机，字符串类型。

**起始版本：** 24

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Web.Webview.Core

## DESKTOP

```TypeScript
DESKTOP = 'Desktop'
```

PC，字符串类型。

**起始版本：** 24

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Web.Webview.Core

## MOBILE

```TypeScript
MOBILE = 'Mobile'
```

手机，字符串类型。

**起始版本：** 24

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Web.Webview.Core

## EINK

```TypeScript
EINK = 'EInk'
```

墨水屏，字符串类型。

**起始版本：** 24

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Web.Webview.Core

## TABLET

```TypeScript
TABLET = 'Tablet'
```

平板，字符串类型。

**起始版本：** 24

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Web.Webview.Core

## WATCH

```TypeScript
WATCH = 'Watch'
```

手表，字符串类型。

**起始版本：** 24

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Web.Webview.Core

## XR

```TypeScript
XR = 'XR'
```

VR+AR设备，字符串类型。

**起始版本：** 24

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Web.Webview.Core
