# ActionSheetOffset

弹窗相对alignment所在位置的偏移量。

**起始版本：** 18

**系统能力：** SystemCapability.ArkUI.ArkUI.Full

## 导入模块

```TypeScript
```

## dx

```TypeScript
dx: number | string | Resource
```

弹窗相对于对齐位置dx的偏移量。可指定像素单位，如'10px'，也可设置百分比字符串，如'100%'。  
**说明：**不指定像素单位时，默认单位vp，如'10'，等同于10。

**类型：** number \| string \| Resource

**起始版本：** 18

**模型约束：** 此接口仅可在Stage模型下使用。

**原子化服务API：** 从API版本18开始，该接口支持在原子化服务API中使用。

**系统能力：** SystemCapability.ArkUI.ArkUI.Full

## dy

```TypeScript
dy: number | string | Resource
```

弹窗相对于对齐位置dy的偏移量。可指定像素单位，如'10px'，也可设置百分比字符串，如'100%'。  
**说明：**不指定像素单位时，默认单位vp，如'10'，等同于10。

**类型：** number \| string \| Resource

**起始版本：** 18

**模型约束：** 此接口仅可在Stage模型下使用。

**原子化服务API：** 从API版本18开始，该接口支持在原子化服务API中使用。

**系统能力：** SystemCapability.ArkUI.ArkUI.Full
